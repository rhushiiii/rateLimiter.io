# Redis connection (default: local Redis)
REDIS_URL=redis://localhost:6379

# Server port
PORT=3000

# Node environment
NODE_ENV=development

# Failure mode when Redis is unavailable: fail_open | fail_closed
FAIL_STRATEGY=fail_open
